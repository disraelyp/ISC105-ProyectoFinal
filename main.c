#include "jugador.c"
#include "tablero.c"

int main() {
    //menu();
    inicio();
    //jugador a, b;
    //creacion_jugadores(&a, &b);
    //tablero cont = generar_tablero(&a, &b);
    //imprimir_tablero(cont);
    //int klk = juego(&a, &b, &cont);
    return 0;
}
